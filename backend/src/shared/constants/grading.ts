export const DEFAULT_WEIGHTS = {
  WW: 0.20,
  PT: 0.60,
  QA: 0.20,
} as const;

export const PASSING_GRADE = 75;

export const HONOR_THRESHOLDS = {
  WITH_HIGHEST_HONORS: 98,
  WITH_HIGH_HONORS: 95,
  WITH_HONORS: 90,
} as const;

export const MAX_SYNC_RETRIES = 3;
